[Unit]
Description=MQTT prod container
Wants=syslog.service

[Service]
TimeoutStartSec=0
Restart=Always
ExecStart=/bin/podman start -a {{ mqtt_container }}
ExecStop=/bin/podman stop --ignore -t 30 {{ mqtt_container }}

[Install]
WantedBy=multi-user.target